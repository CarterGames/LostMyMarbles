using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class MenuItems : MonoBehaviour {


	[MenuItem("J Tools/Set to 0,0,0")]
	
	static void SetTo000(/*MenuCommand command*/)
	{
		/*
		Vector3 Pos = new Vector3(0, 0, 0);
		Transform transform = (Transform)command.context;

		.transform.SetPositionAndRotation(Pos, Quaternion.Euler(0, 0, 0));
		Selection.activeObject.
		Debug.Log("Position Set to 0,0,0");
		*/
	}
	
}
